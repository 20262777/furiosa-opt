target/

# Ground truth is regenerated by `./pipeline.sh gt <op>` from the torch scripts in
# src/ops/<op>/ground_truth.py, so it is reproducible and not worth versioning.
data/

# Run logs are per-machine noise; report.md and schedule.json are kept.
results/*/test.log
results/*/schedule.log
